[user]
  name = David Smith
  email = codeactual@gmail.com
[alias]
  amend = commit --amend -C HEAD
  br = branch
  cdiff = diff --cached
  ci = commit
  clog = log --stat -n 1
  co = checkout
  difflast = diff HEAD^ HEAD
  ignored = ls-files --others -i --exclude-standard
  l = log --oneline --graph --decorate
  logmasterin = log origin/master ^master
  logmasterout = log master ^origin/master
  modified = ls-files -m
  sst = status -sb
  st = status
  timeline = log --graph --decorate --branches --pretty=oneline
  unstage = reset HEAD
  untracked = ls-files -o --exclude-standard
  wdiff = diff --word-diff
[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto
  ui = true
[github]
  user = codeactual
[help]
  autocorrect = 1
